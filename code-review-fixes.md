# Code Review Fixes — alphaTab

## Проблема: плейхед замирал после нескольких запусков Perform
| # | Файл | Изменение | Результат |
|---|------|-----------|-----------|
| 1 | `PerformPanel.ts:455` | `restorePlayback()` вызывает `api.stop()` вместо `api.pause()` | **Главный фикс.** `stop()` сбрасывает секвенсор, `pause()` — нет. После сессии секвенсор оставался в конце диапазона и не запускался повторно |
| 2 | `PerformPanel.ts:396-407` | Снепшот сохраняет/восстанавливает `metronomeVolume` и `countInVolume` | Предотвращает накопление perform-значений в API |
| 3 | `PlaygroundSidePanel.ts:1528-1535` | Автосохранение заменено на ручную кнопку «Save Settings» | Исключает запись испорченных значений в localStorage во время сессии |

## Проблема: дребезжащий звук при удержании клавиш в Perform/Practice
| # | Файл | Изменение | Результат |
|---|------|-----------|-----------|
| 4 | `AlphaSynth.ts:437-439` | `playLiveNote()` не вызывает `output.play()` когда score уже играет | Устраняет двойной аудио-узел параллельной обработки |
| 5 | `AlphaSynth.ts:437` | Возвращён `_notPlayedSamples = 0` в `playLiveNote()` | Защищает живые ноты от убийства в `checkForFinish` |

## Проблема: мерцание хинтов в Perform
| # | Файл | Изменение | Результат |
|---|------|-----------|-----------|
| 6 | `PracticeOverlay.ts:105-114` | `showItem()` кеширует последний item, пропускает перерисовку | DOM не пересоздаётся каждый кадр |
| 7 | `PianoKeyboard.ts:628-630` | `setHintNotes()` сравнивает с текущими нотами | Классы не снимаются/вешаются попусту |
| 8 | `PianoKeyboard.ts:709-719` | `applyHintNotes()` точечно обновляет изменившиеся клавиши | Нет массового сброса `.hint-highlight` |
| 9 | `PerformPanel.ts:602-622` | `selectHintItem()` — фолбек без wall-таймстемпов при старте | Хинты видны с первого кадра |
| 10 | `PerformPanel.ts:579-586` | При `null` из `selectHintItem` хинты не стираются | Нет мигания on/off |

## Проблемы UI/UX
| # | Файл | Изменение | Результат |
|---|------|-----------|-----------|
| 11 | `PlaygroundSidePanel.ts:342-343` | Убраны 4 инлайн-стиля из `setMode()` | Боковая панель выезжает плавно |
| 12 | `TrackItem.ts:458-465` | `setActive()` вызывает `onTrackSelect()` с защитой от рекурсии | Клик по треку рендерит его на стане |
| 13 | `PianoKeyboard.ts:771-782` | `resolveShortcutMidi()` — фолбек на центр клавиатуры вместо октавы 5 | Горячие клавиши ближе к реальным нотам |
| 14 | `PerformPanel.ts:422-423` | Метроном и count-in = 10% при старте Perform | Слышен ритм, есть время увидеть хинт |

## Проблемы: утечки и лишняя работа
| # | Файл | Изменение | Результат |
|---|------|-----------|-----------|
| 15 | `TrackItem.ts:551-585` | Подписки MIDI-загрузки чистятся в `dispose()` | Нет утечки при закрытии |
| 16 | `TrackItem.ts:583-585` | Подписка на `error` заменена на 10с таймаут | Посторонние ошибки не ломают загрузку |
| 17 | `TrackItem.ts:420` | Убран `loadMidiForScoreDynamic()` из слайдера баланса | Нет регенерации MIDI 60 раз/сек |
| 18 | `PlaygroundSidePanel.ts:1182-1184` | Убран сброс громкости видимых треков в 1 | Пользовательская громкость сохраняется |
| 19 | `PerformPanel.ts:373-376` | `connectMidi()` ловит ошибки, защита от гонки | Нет UnhandledPromiseRejection |
| 20 | `PerformPanel.ts:466-467` | Двойной цикл mute/solo смержен в один | Микрооптимизация |
| 21 | `PracticeController.ts:205` | `cacheTick > 0` → `>= 0` | Корректная обработка первого бита |
